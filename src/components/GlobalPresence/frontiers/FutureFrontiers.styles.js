import styled from "styled-components";

export const FrontierContent = styled.div`
  width: 100%;
  max-width: 76rem;
  color: #ffffff;
  font-size: clamp(1rem, 1.45vw, 1.55rem);
  font-weight: 600;
  line-height: 1.6;
`;

export const FrontierLine = styled.p`
  margin: 0;
`;

export const FrontierRegion = styled.span`
  font-weight: 700;
`;

export const FrontierText = styled.span`
  font-weight: 500;
`;
